{
  "name": "ciphernumbers-contracts",
  "version": "1.0.0",
  "description": "Solidity contracts for CipherNumbers FHE demo",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network zama-devnet",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.3.1",
    "fhevm": "^0.3.0",
    "hardhat": "^2.17.0"
  }
}
